services:
  - type: web
    name: one-percent-club-server
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: CLIENT_ORIGIN
        sync: false
